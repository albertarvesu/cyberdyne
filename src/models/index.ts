import { ERobotStatus, IRobot, IRobotAppState, IRobotData } from './robot';

interface IAppState {
  robots: IRobotAppState;
}

export {
  ERobotStatus,
  IAppState,
  IRobot,
  IRobotAppState,
  IRobotData,
}